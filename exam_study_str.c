#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{   
    char strA[100];
    char strB[100];
    char buffer[100];
    /*while((str = getchar()) != '\n')
    {
        putchar(str);
    }*/

/*    gets(strA);
    gets(strB);

    int i = atoi(strA);
    double j = atof(strB);
    double result = i + j;

    sprintf(buffer, "%lf", result);
    printf("%s", buffer);
*/
    /*char word[30];
    gets(word);

    char *ptr = strtok(word, " ");

    while(ptr != NULL)
    {
        printf("%s\n", ptr);
        ptr = strtok(NULL, " ");
    } */

    char wordA[30];
    char wordB[30];

    gets(wordA);
    gets(wordB);
    
    

}